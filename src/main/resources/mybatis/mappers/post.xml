<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">
	<!-- Vo 가져오기 -->
	<select id = "select" resultType="com.javaex.vo.PostVo" parameterType="String">
		<![CDATA[
			select  b.id
			        ,b.blogTitle
			        ,b.logoFile
			from    users u, blog b
            where   u.id = b.id
			and	b.id = #{id}
		]]>
	</select>
	
	<!-- post 정보 추가 -->
	<insert id = "insert" parameterType="com.javaex.vo.PostVo">
		<![CDATA[
			INSERT INTO post 
			VALUES (seq_post_no.nextval, seq_category_no.nextval, #{postTitle}, #{postContent}, sysdate)
		]]>
	</insert>
	
	
</mapper>